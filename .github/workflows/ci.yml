name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  desktop:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux
            runner: ubuntu-22.04
          - name: macOS
            runner: macos-latest
          - name: Windows
            runner: windows-latest

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Install Linux desktop dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            libfuse2 \
            patchelf \
            rpm \
            xdg-utils

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Install Bun
        uses: oven-sh/setup-bun@v2

      - name: Install just
        uses: taiki-e/install-action@v2
        with:
          tool: just@1.51.0
          fallback: none

      - name: Install frontend dependencies
        run: just install

      - name: Validate dependency-light native core
        run: just native-check

      - name: Run checks and tests
        run: just check

      - name: Validate Linux statx accounting and cancellation
        if: runner.os == 'Linux'
        shell: bash
        run: |
          fixture="$RUNNER_TEMP/cepa-statx-fixture"
          just benchmark-fixture "$fixture" 32 128 4096
          printf 'hard-link fixture\n' > "$fixture/hard-link-source.bin"
          ln "$fixture/hard-link-source.bin" "$fixture/group-0000/hard-link-copy.bin"
          ln -s /etc "$fixture/external-directory-link"
          just validate-scan "$fixture" jwalk statx
          just benchmark-cancellation "$fixture" statx 3 1024

      - name: Build native application
        run: just build

      - name: Build and validate Linux distribution bundles
        if: runner.os == 'Linux'
        env:
          APPIMAGE_EXTRACT_AND_RUN: "1"
        run: |
          just bundle
          just validate-linux-bundles
          (
            cd src-tauri/target/release/bundle
            tar -czf "$RUNNER_TEMP/cepa-linux-bundles.tar.gz" \
              deb/*.deb rpm/*.rpm appimage/*.AppImage
          )

      - name: Retain Linux distribution bundles
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v7
        with:
          name: cepa-linux-${{ github.sha }}
          path: ${{ runner.temp }}/cepa-linux-bundles.tar.gz
          if-no-files-found: error
          retention-days: 14
